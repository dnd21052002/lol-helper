export function BuildImporterPage(): JSX.Element {
  return (
    <div>
      <h2 style={{ marginTop: 0 }}>Build / Rune Importer</h2>
      <p style={{ color: 'var(--text-dim)' }}>
        Import build và rune vào client với 1 click. Đang phát triển...
      </p>
    </div>
  );
}
